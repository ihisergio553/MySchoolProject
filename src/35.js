// Import necessary packages if any
// No imports needed for this simple example

// Create an object to store your data
var myData = {
  "students": [
    { name: "Alice", age: 13 },
    { name: "Bob", age: 15 }
  ],
  "grades": [ // Assuming you have grades stored here, replace with actual values or dynamically generated
    {"studentName": "Charlie", grade: 90},
    {"studentName": "David", grade: 85}
  ]
};

// Print the data to the console
console.log(myData);
